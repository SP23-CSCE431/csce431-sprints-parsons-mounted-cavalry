<%= link_to "Back to users", users_path %>

<div class="horses index">
    <h1>Horses</h1>

    <%= link_to("<button>Add New Horse</button>".html_safe, new_horse_path, class: 'action new') %>
    <br><br>
    <table class="listing" summary="Horse list" style="width: 30%;" border = "1">
        <tr class="header">
            <th>Name</th>
            <th>Brand</th>
            <th>Details</th>
        </tr>
    
    <% @horses.each do |horse| %>
    <tr>
        <td><center><%= horse.name %></center></td>
        <td><center><%= horse.brand %></center></td>
        <td class="Details">
            <center>
            <%= link_to("Show", horse_path(horse), class: 'action show') %>
            </center>
        </td>
    </tr>
    <% end %>
    </table>
</div>
