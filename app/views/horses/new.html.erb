<body class="align-items-center justify-content-center" style="overflow-x: hidden; overflow-y: hidden;">
  <%= render(NavbarComponent.new())%>

  <%# Sidebar %>
  <%= render(SidebarAdminsComponent.new())%>
  <div class="justify-content-center align-items-center mx-auto text-center">
    <div class="horses new">
        <br>
        <h1>Add New Horse</h1>

        <%= form_for(@horse) do |f| %>
        <% # url: horses_path, method: 'post' %>

        <%= render(partial: 'form', locals: {f: f})%>

        <% end %>
        
        <br>
        <%= link_to("<button type='button' class='btn btn-secondary create'>Back to Horses</button>".html_safe, admins_horses_path, :class => 'back-link') %>
    </div>
  </div>
</body>