<div id="<%= dom_id schedule %>">
  <p>
    <strong>User:</strong>
    <% user = User.find(schedule.user_id) %>
    <%= user.first_name %> <%= user.last_name %>
  </p>

  <p>
    <strong>Recurrence:</strong>
    <%= recurrence_str(schedule.recurrence) %>
  </p>

</div>
