<table class="listing" summary="Cadets Report">
    <tr class="header">
        <th>Cadet</th>
        <th>Date</th>
        <th>Infraction</th>
    </tr>

    <% @cadets.each do |cadet| %>
    <tr>
        <td><center><%= "#{cadet.first_name} #{cadet.last_name}" %></center></td>
        <td><center><%=  %></center></td>
        <td><center><%=  %></center></td>
    </tr>
    <% end %>
</table>

<br>

<table class="listing" summary="Staffs Report">
    <tr class="header">
        <th>Command Staff</th>
        <th>Date</th>
        <th>Infraction</th>
    </tr>

    <% @staffs.each do |staff| %>
    <tr>
        <td><center><%= "#{staff.first_name} #{staff.last_name}" %></center></td>
        <td><center><%=  %></center></td>
        <td><center><%= "Absent for " %></center></td>
    </tr>
    <% end %>
</table>


<style>
    tr.header {
        background-color: #A7A7A7;
    }

    table.listing tr:not(.header) {
        background-color: #D1D1D1;
    }

    table.listing {
        width: 65%;
        margin: 0 auto;
        border-collapse: collapse;
    }

    td, th {
        border: 1px solid white;
    }
</style>